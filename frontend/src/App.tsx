import KitchenSink from '@/pages/_dev/KitchenSink';

/**
 * Phase 1 placeholder entry point. The router + AppShell arrive in
 * Step 03 (03-app-shell.md) — for now we render the design-system
 * kitchen sink so the client can sign off on tokens and primitives.
 */
export default function App() {
  return <KitchenSink />;
}
